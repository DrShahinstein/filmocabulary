# Install this manifest in addition to the cross-platform application stack.
-r requirements.txt

# The Home production server. Uvicorn serves the ASGI application and runs on
# every platform, including Windows.
uvicorn>=0.30,<1
